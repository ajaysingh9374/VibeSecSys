PROJECT CONTEXT (DO NOT CHANGE)
-Project: VibeSecSys
-Goal: Build a working cybersecurity application
-Development method: PROMPTS → CODEX BUILDS → we validate
-Strict step-by-step execution (NO scope jumps)
-Co-development with CodeX + K2509118 (Myself)

REPOSITORY STRUCTURE - No modification to folder structure untill specifically asked. Detail are as below
scanner/     : XML parsing and scan handling
samples/     : Sample Nmap XML files
reports/     : Output reports (final stage)
ai/          : AI integration module
docs/        : Notes / logs
prompts/     : Stored prompts (MANDATORY)
main.py      : Entry point

CURRENT STAGE
-Stage: 5 — AI Integration
-Step: 5.3 — Prompt embedding

SAVE PROMPT and UPDATE DOCUMENTATION (Important)
-Save as: prompts/stage5-step3-prompt-embedding.txt

RULES (STRICT): Only follow what is required in this step

TASK: Embed the AI prompt template inside the application.

Requirements:
1. Update ai/ai_engine.py:
   Inside generate_ai_analysis(ai_input: str):
   -Create a multi-line string (prompt_template)
   -The prompt must include:
     "You are a cybersecurity analyst.
     Analyze the following scan results and provide:
     -Summary of findings
     -Identified risks
     -Misconfigurations
     -Recommendations
     Scan Data:
     {ai_input}
     "
2. Replace placeholder return with:
   -Return the full formatted prompt string (NOT AI output yet)
3. Print:
   -"AI prompt prepared"
4. Update main.py:
   -Print the returned prompt (or first few lines for readability)
5. Ensure:
   -ai_input is correctly inserted into the prompt
   -Output is clean and readable

EXPECTED OUTPUT
-Running 'python main.py' prints selected scan mode 
- Prompt clearly includes: instructions and scan data from previous stage
